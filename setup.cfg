[metadata]
name = fastapi_rag
version = 0.1.0
description = FastAPI RAG chat application

[options]
packages = find:            # picks up src.agent, src.utils, …
py_modules = main           # ← this line exposes main.py
python_requires = >=3.10
install_requires = -r requirements.txt
